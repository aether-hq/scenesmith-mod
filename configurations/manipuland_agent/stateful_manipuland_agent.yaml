# Stateful manipuland agent configuration with planner/designer/critic trio

defaults:
  - base_manipuland_agent

# Agent configuration
agents:
  designer_agent:
    name: "designer"
    max_turns: 20
    prompt: MANIPULAND_DESIGNER_AGENT

  planner_agent:
    name: "planner"
    max_turns: 8
    prompt: MANIPULAND_PLANNER_AGENT

  critic_agent:
    name: "critic"
    max_turns: 6
    prompt: MANIPULAND_CRITIC_AGENT
